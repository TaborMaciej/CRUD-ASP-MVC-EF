@model IEnumerable<lista7_zad1.Models.Professor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@if (User.IsInRole("Admin"))
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AlbumNr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.People)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AlbumNr)
            </td>
            <td>
                @item.People?.OsobaId - @item.People?.Name @item.People?.Surname
            </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.ProfessorID"> Details </a>
                    @if (User.IsInRole("Admin"))
                    {
                        <span>&nbsp;|&nbsp;</span>

                        <a asp-action="Edit" asp-route-id="@item.ProfessorID">Edit</a>

                        <span>&nbsp;|&nbsp;</span>
                        <a asp-action="Delete" asp-route-id="@item.ProfessorID"> Delete</a>
                    }
                </td>
        </tr>
}
    </tbody>
</table>
